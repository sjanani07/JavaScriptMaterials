//types of functions
//1.named function

function myName(){
    let myName = "Janani"
    console.log("myName is "+myName);
}

myName();

//2. named function with param

function add(a,b){
    let c = a *b;
    console.log("c "+c);
}
add(2,7)

// Method overload is not possible in JS
//Method Override is possible in JS
