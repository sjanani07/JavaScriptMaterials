let langName = "Java";
langName = "Python"; // override in memory.
//let langName = "C#"; //Eg: for one roll number - two persons cannot be assigned. Problem. so use let.
//when we declare a type, then it considers it as new variable and creates a memeory for it.
//only in var it is possible and it becomes problem. so use let.

function logic(){
    let langName = "DotNet"
    console.log(langName);
}

console.log(langName);
logic();

let given = 't';
console.log(given.length);

// if(given.length===1){
//    let character = given;
//     console.log("character "+ character);
// }
// else {
//     let string = given;
//     console.log("string "+ string);
// }

